#include <iostream>
#include "config.h"
#include "RunningSystem.h"
int main() {

//    FILE *diskFile = fopen("disk", "wb+");
//    struct super_block* sb = (struct super_block*)malloc(sizeof(struct super_block));
//    sb->s_block_size = 26;
//    sb->s_dinode_size = 4;
//    fseek(diskFile,BLOCKSIZ,SEEK_SET);
//    fwrite(sb, sizeof(struct super_block), 1, diskFile);
//    fclose(diskFile);


    return 0;
}
